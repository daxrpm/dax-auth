# --- Rust ---
target/
**/*.rs.bk
*.pdb
Cargo.lock.bak

# --- Editors / IDE ---
.vscode/
.idea/
.opencode/
*.swp
.DS_Store

# --- Project artifacts ---
# ONNX model files are fetched at install/build time, never committed.
models/
*.onnx
# Local biometric data must never leak into VCS.
*.template
*.vault

# --- Test / coverage outputs ---
*.log
*.tmp
